__pycache__/

# The only file mapping real people to teams. Everything else uses team names
# and initials, so it's safe in a public repo — this isn't.
# Format: {"identities": {"<draft owner name>": {"key": "AB", "team": "..."}}}
shadow/data/identities.json
